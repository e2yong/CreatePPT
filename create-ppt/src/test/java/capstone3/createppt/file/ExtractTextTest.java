package capstone3.createppt.file;

import org.junit.jupiter.api.Test;

import static capstone3.createppt.extract.ExtractText.*;
import static capstone3.createppt.file.PathConst.*;

class ExtractTextTest {
    @Test
    void extract() {
        // String inputFilePath = UPLOAD_PATH + "";
        // String outputFilePath = EXTRACT_PATH + "";

        // ExtractText.extractText(inputFilePath, outputFilePath);
        // extractTextAndImage(inputFilePath, outputFilePath);
    }
}